
function change(select, divId) {
    var selectedValue = select.value;
    document.getElementById(divId).style.background = selectedValue;
}

function change(select, divId) {
    var selectedValue = select.value;
    document.getElementById(divId).style.background = selectedValue;
}

function change(select, divId) {
    var selectedValue = select.value;
    document.getElementById(divId).style.background = selectedValue;
}


